#' @param scenarios A matrix of response rate scenarios. Each column corresponds
#'   to a scenario and each row corresponds to a basket. A default scenario
#'   matrix can be created with \code{\link{get_scenarios}}.
